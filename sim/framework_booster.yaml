# Arena sim2sim via booster-motion DDS path
# Volumes with empty/relative host path are resolved relative to PIXI_PROJECT_ROOT.
image: arena/arena-booster
volumes:
  - ":/workspace"
  - "/dev/shm/circus_ipc:/dev/shm/circus_ipc"
